import { CommonTemplate } from "@/components/layout/commonTemplate";
import { ButtonDemo } from "../button/button-component";
import { BreadcrumbDemo } from "./breadcrumb-component";

export const pageTitle ="Breadcrumbs"

export const pageDescription = "Displays the path to the current resource using a hierarchy of links."

export const mainDemo = {
    id: "main",
}

export const demos = [
    {
        id: "separator",
        title: "Custom separator",
        description: "Use a custom component as children for <BreadcrumbSeparator /> to create a custom separator."
    },
    {
        id: "dropdown",
        title: "Dropdown",
        description: "You can compose <BreadcrumbItem /> with a <DropdownMenu /> to create a dropdown in the breadcrumb."
    },
    {
        id: "collapsed",
        title: "Collapsed",
        description: "We provide a <BreadcrumbEllipsis /> component to show a collapsed state when the breadcrumb is too long."
    },
    {
        id: "link",
        title: "Link component",
        description: "To use a custom link component from your routing library, you can use the asChild prop on <BreadcrumbLink />."
    },
];

export const ButtonConfig = {
    config: {
        demos,
        mainDemo
    }
};

export const installationCodeList = [
    { label: "npm", code: "npx next-forge@latest init" },
    { label: "yarn", code: "yarn next-forge@latest init" },
    { label: "pnpm", code: "pnpm next-forge@latest init" },
    { label: "bun", code: "bunx next-forge@latest init" }
];

export const usageCodeList = [
    { code: `import {
    Breadcrumb,
    BreadcrumbItem,
    BreadcrumbLink,
    BreadcrumbList,
    BreadcrumbPage,
    BreadcrumbSeparator,
} from "@/components/ui/breadcrumb"` }, 
    { code: `<Breadcrumb>
    <BreadcrumbList>
        <BreadcrumbItem>
            <BreadcrumbLink href="/">Home</BreadcrumbLink>
        </BreadcrumbItem>
        <BreadcrumbSeparator />
        <BreadcrumbItem>
            <BreadcrumbLink href="/components">Components</BreadcrumbLink>
        </BreadcrumbItem>
        <BreadcrumbSeparator />
        <BreadcrumbItem>
            <BreadcrumbPage>Breadcrumb</BreadcrumbPage>
        </BreadcrumbItem>
    </BreadcrumbList>
</Breadcrumb>` }
];

<CommonTemplate 
    pageTitle={pageTitle} 
    pageDescription={pageDescription}
    installationCommands= {installationCodeList}   
    usageCommands={usageCodeList}
    config={ButtonConfig.config}
>
  <BreadcrumbDemo />
</CommonTemplate>